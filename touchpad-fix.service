[Unit]
Description=Fix touchpad after resuming suspend
After=suspend.target

[Service]
Type=oneshot
ExecStart=/usr/bin/modprobe -r hid_rmi
ExecStart=/usr/bin/modprobe hid_rmi

[Install]
WantedBy=suspend.target
